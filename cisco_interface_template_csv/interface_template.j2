
! Generated Configurations
interface {{ interface }}
    description Link to {{ server }} port {{ link }} for {{ purpose }}
    switchport 
    {%-  if vlan == 'trunk' %}
    switchport mode trunk
    {% else %}
    switchport mode access
    switchport access vlan {{ vlan }}
    spanning-tree type edge
    {% endif -%}
    no shutdown

